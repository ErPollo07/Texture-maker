// This page allows the user to create a new project.
import React from 'react';

const CreateProject: React.FC = () => {
  return (
    <div>
      <h2>Crea un nuovo progetto</h2>
      {/* Qui andrà il form per la creazione del progetto */}
    </div>
  );
};

export default CreateProject;
