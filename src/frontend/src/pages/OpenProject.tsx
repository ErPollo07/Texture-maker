// Questa pagina permette all'utente di creare un nuovo progetto.
import React from 'react';

const OpenProject: React.FC = () => {
  return (
    <div>
      <h2>Apri un progetto</h2>
      {/* Qui andrà il form per la creazione del progetto */}
    </div>
  );
};

export default OpenProject;
